export { MaskedInput } from './src/MaskedInput'

/**
 * Usage Example:
 *
 * import { MaskedInput } from 'react-text-mask-modern';
 *
 * <MaskedInput
 *   mask={[/\d/, /\d/, '/', /\d/, /\d/, '/', /\d/, /\d/, /\d/, /\d/]}
 *   placeholder="dd/mm/yyyy"
 *   value={value}
 *   onChange={(e) => setValue(e.target.value)}
 * />
 */

